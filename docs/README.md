# OpenBroker Docs

This folder contains the initial specifications for OpenBroker.

The current implementation is an MVP 1 scaffold. Treat `docs/specs` as the source of truth for product boundary, security rules, API shape, broker adapter contracts, deployment operations, and future AI/MCP integration.

Practical setup guides live in `docs/guides`.
